# Tier 2 — Training Dynamics

Vectorized NumPy, then the bridge to PyTorch. Create experiment folders here as
you reach them (mirror the e01 layout: `README.md` + `run.py` + `notes.md`):

- [x] **e09** optimizers (SGD → Momentum → Adam)
- [x] **e10** initialization (Xavier/He) & signal scale
- [x] **e11** regularization (L2, over/underfitting)
- **e12** MNIST from scratch (pure-NumPy MLP)  ← first "real" result
- **e13** reproduce e12 in PyTorch; compare

See [`../../ROADMAP.md`](../../ROADMAP.md).
