# Tier 3 — Architectures (PyTorch)

- **e14** CNN (LeNet) on MNIST
- **e15** RNN/LSTM char-level model
- **e16** embeddings (mini word2vec)
- **e17** seq2seq + the attention bottleneck (sets up Tier 4)

From here you use PyTorch. If `torch` won't install on Python 3.14, use a 3.12
venv (see `requirements.txt`). See [`../../ROADMAP.md`](../../ROADMAP.md).
