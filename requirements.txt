# --- GUI / deploy: Streamlit Community Cloud installs exactly these ---
streamlit>=1.36
numpy>=1.26
matplotlib>=3.8
pandas>=2.0
scikit-learn>=1.4   # M8 "ML in Python" — live worked examples

# Testing (local only)
pytest>=8.0

# ----------------------------------------------------------------------------
# Later tiers — install when you reach them. PyTorch wheels can lag the newest
# Python; if `pip install torch` fails on 3.14, create a separate 3.12 venv:
#     python3.12 -m venv .venv-dl && source .venv-dl/bin/activate
# ----------------------------------------------------------------------------
# torch>=2.2
# torchvision>=0.17
# transformers>=4.40
# datasets>=2.19
# tokenizers>=0.19
