# Seviye 1 Soruları — Backprop ve Autograd

Koda bakmadan önce bunları sesli cevapla.

1. `backward()` neden çıktı gradyanını `1.0` yaparak başlar?
2. Hesaplama grafiğinde topolojik sıra neden gerekir?
3. Gradyanlar neden atama ile değil `+=` ile birikir?
4. `d = a*b + c` için neden `dd/da = b`, `dd/db = a`, `dd/dc = 1`?
5. Yerel türev kuralı neyi bilir, neyi bilmez?
6. ReLU neden sadece aktif tarafta gradyan geçirir?
7. Bir MLP XOR'u neden çözebilir?
8. `zero_grad()` tam olarak neyi engeller?
9. Kendinden emin ama yanlış sınıflandırmada cross-entropy neden MSE'den hızlı öğrenir?
10. Gradient checking neyi kanıtlar, neyi kanıtlamaz?

